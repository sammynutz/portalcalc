services:
  - type: web
    name: portalcalc-license-api
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn license_server:app --host 0.0.0.0 --port $PORT
    healthCheckPath: /health
